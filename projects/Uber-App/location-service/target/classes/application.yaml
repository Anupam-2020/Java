# Application

spring:
  application:
    name: location-service

server:
  port: 8082

# Redis Configuration.
  data:
    redis:
      host: localhost
      port: 6379

# Actuator
management:
  endpoints:
    web:
      exposure:
        include: health, info
